# ============================================================
# JARVIS OS — Dependências Python
# Instale com: pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==12.0

# --- Claude API ---
anthropic==0.34.2

# --- Supabase ---
supabase==2.7.4
postgrest==0.17.1

# --- Voice: STT (Whisper) ---
faster-whisper==1.0.3
# Alternativa nativa C++: pywhispercpp (requer build manual)
# Para produção em CPU, faster-whisper é mais estável

# --- Voice: Wake Word ---
openwakeword==0.6.0

# --- Voice: TTS (Piper via subprocess — binário separado) ---
# Piper é chamado via subprocess; não há pacote pip oficial estável
# O binário é baixado no setup.sh

# --- Voice: Áudio ---
pyaudio==0.2.14
sounddevice==0.4.7
soundfile==0.12.1
numpy==1.26.4

# --- Embeddings (local, gratuito) ---
sentence-transformers==3.1.1
torch==2.4.1
# CPU-only torch para economizar espaço (opcional, substitua pela linha acima):
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.4.1+cpu

# --- Utilitários ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0
httpx==0.27.2
aiofiles==24.1.0
asyncio-throttle==1.0.2

# --- Processamento de documentos (Delivery Manager) ---
reportlab==4.2.2       # Geração de PDF
markdown==3.7          # Renderização de MD

# --- Agendamento e tarefas ---
asyncio==3.4.3; python_version < "3.10"

# --- Logging estruturado ---
structlog==24.4.0
